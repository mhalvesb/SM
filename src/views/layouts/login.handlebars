<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/login.css">
    <title>Login</title>
    
</head>
<body>
    <main>
            <div class="alert-area">
            {{#if error}}
            <div class="alert">
                <h5>{{error}}</h5>
            </div>
            {{else}}
        {{/if}}
        </div>
        <div class="alert-area">
            {{#if success_msg}}
            <div class="success">
                <h5>{{success_msg}}</h5>
            </div>
            {{else}}

            {{/if}}
        </div>
        
            
    <div class="box">
            <form action="/auth/login" method="post">
            <h1>Faça seu Login</h1>
            <div class="usernameinput">
                <img src="/imagens/icons/username.png" alt="username" class="username">
                
                <input type="text" name="login" class="login" required="required">
                <span>Usuário</span>
            </div>
            <div class="passwordinput">
                <img src="/imagens/icons/trancar.png" alt="password" class="password" >
                <input type="password" name="senha" class="pass" required="required">
                <span>Senha</span>
            </div>
            <button type="submit">Entrar</button>
            
            </form>
            <div class="logfoo">
               Não é registrado? <a href="/auth/registro">Registrar-se agora</a>
            </div>
    </div>
</div>
        </main>
</body>
</html>